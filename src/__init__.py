# -*- coding: utf-8 -*-
"""地面站版本號的單一來源。

## 為什麼需要這個檔

2026-08-07 交接盤點時發現版本號有三套，沒有一套是對的：

  · GUI 右下角寫死 `v1.0.5`  —— micro:bit 原型時代的編號，從沒改過
  · README 標題寫 `v3.4`      —— changelog 停在 07-30，之後還有 27 個 commit
  · GitHub Release 標 `v3.0`  —— 落後 67 個 commit

實際傷害：發射日修好的東西（例如「單一 KH 突波會永久灌水回報頂點高度」），
下一屆從 GUI 看到 `1.0.5`、從 Release 看到 `v3.0`，兩邊都查不到那個修復，
會以為 bug 還在、或以為自己裝錯版本。

**所以版本號只留這一個定義，GUI 與 README 都從這裡讀。**

## 版本規則

`v3.x` 是接上真實航電之後的 release track；`1.0.x` 是 micro:bit 原型時代的
內部編號，兩套不連續，不要試圖對齊。改動之後記得同時更新 README 的 changelog。
"""

__version__ = "3.5.0"

# 這一版包含 2026-08-01 發射日當天的全部修復。
# 那天的關鍵幾項（完整清單見 README changelog）：
#   · 單一 KH 突波會永久灌水回報的頂點高度
#   · 任何在發射台上的震動都可能把 IGNITION 卡住
#   · 飛行時間軸改用火箭的時鐘，不是接收時間
#   · 沒有 GPS fix 時不要憑空捏造座標
#   · 遙測 CSV 會靜默掉列（三種途徑）
